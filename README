init version was forked from webkitgtk-1.8.2(stable version)

Motivation:
0. configure the build argmenuts to remove no useful function in deepin-desktop
1. add the ability of access system desktop
2. remove some security limit in normal web, but useful(even essential) functional on desktop
3. add other functional to help develop.
